name: CI/CD — eSoccer Dashboard (API + Frontend)

on:
  push:
    branches: [api-dash]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_API: ghcr.io/${{ github.repository }}/api
  IMAGE_FRONTEND: ghcr.io/${{ github.repository }}/frontend

concurrency:
  group: deploy-esoccer-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build & Push Images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Gerar tag do commit
        id: meta
        run: echo "short_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Login no ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers (API)
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache-api
          key: ${{ runner.os }}-buildx-api-${{ github.sha }}
          restore-keys: ${{ runner.os }}-buildx-api-

      - name: Build & Push API
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_API }}:${{ steps.meta.outputs.short_sha }}
            ${{ env.IMAGE_API }}:latest
          cache-from: type=local,src=/tmp/.buildx-cache-api
          cache-to: type=local,dest=/tmp/.buildx-cache-api-new,mode=max

      - name: Cache Docker layers (Frontend)
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache-frontend
          key: ${{ runner.os }}-buildx-frontend-${{ github.sha }}
          restore-keys: ${{ runner.os }}-buildx-frontend-

      - name: Build & Push Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./esoccerdashboard
          file: ./esoccerdashboard/Dockerfile
          push: true
          build-args: |
            VITE_API_BASE_URL=https://api-tg-esoccer.arvsystems.cloud
            VITE_API_KEY=${{ secrets.VITE_API_KEY }}
          tags: |
            ${{ env.IMAGE_FRONTEND }}:${{ steps.meta.outputs.short_sha }}
            ${{ env.IMAGE_FRONTEND }}:latest
          cache-from: type=local,src=/tmp/.buildx-cache-frontend
          cache-to: type=local,dest=/tmp/.buildx-cache-frontend-new,mode=max

      - name: Mover caches
        run: |
          rm -rf /tmp/.buildx-cache-api /tmp/.buildx-cache-frontend
          mv /tmp/.buildx-cache-api-new /tmp/.buildx-cache-api
          mv /tmp/.buildx-cache-frontend-new /tmp/.buildx-cache-frontend

  deploy:
    name: Deploy to VPS
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read
      packages: read

    steps:
      - name: Validar secrets de deploy
        shell: bash
        run: |
          if [ -z "${{ secrets.VPS_HOST }}" ] || [ -z "${{ secrets.VPS_USER }}" ] || [ -z "${{ secrets.VPS_SSH_KEY }}" ]; then
            echo "Secrets de deploy ausentes (VPS_HOST, VPS_USER, VPS_SSH_KEY)"
            exit 1
          fi

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          timeout: 60s
          command_timeout: 5m
          script: |
            set -e

            IMAGE_API="${{ env.IMAGE_API }}"
            IMAGE_FRONTEND="${{ env.IMAGE_FRONTEND }}"

            resolve_service() {
              expected="$1"
              suffix="$2"
              if docker service inspect "${expected}" >/dev/null 2>&1; then
                echo "${expected}"; return 0
              fi
              found="$(docker service ls --format '{{.Name}}' | grep -E "_${suffix}$" | head -n 1 || true)"
              if [ -n "${found}" ]; then
                echo "${found}"; return 0
              fi
              return 1
            }

            echo "🔐 Login no registry..."
            echo "${{ github.token }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

            echo "📦 Pulling imagens..."
            docker pull "${IMAGE_API}:latest"
            docker pull "${IMAGE_FRONTEND}:latest"

            # --- API ---
            SVC_API="$(resolve_service "api-esoccer_api" "api")" || true
            if [ -n "${SVC_API}" ]; then
              echo "🚀 Atualizando API (${SVC_API})..."
              docker service update --image "${IMAGE_API}:latest" --with-registry-auth --force "${SVC_API}"
            else
              echo "⚠️  Serviço API não encontrado. Crie a stack no Portainer."
            fi

            # --- Frontend ---
            SVC_FRONT="$(resolve_service "api-esoccer_frontend" "frontend")" || true
            if [ -n "${SVC_FRONT}" ]; then
              echo "🚀 Atualizando Frontend (${SVC_FRONT})..."
              docker service update --image "${IMAGE_FRONTEND}:latest" --with-registry-auth --force "${SVC_FRONT}"
            else
              echo "⚠️  Serviço Frontend não encontrado. Redeploy a stack para criá-lo."
            fi

            echo "🧹 Limpando imagens antigas..."
            docker image prune -f

            echo "✅ Deploy concluído!"
