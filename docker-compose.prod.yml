version: "3.8"

services:
  api:
    image: ${IMAGE_API}:latest
    environment:
      - REDIS_URL=redis://redis:6379
      - CACHE_TTL_ANALYSIS=${CACHE_TTL_ANALYSIS:-86400}
      - CACHE_TTL_EXPORT=${CACHE_TTL_EXPORT:-3600}
      - CACHE_TTL_JOB=${CACHE_TTL_JOB:-7200}
      - PRECOMPUTE_WORKERS=${PRECOMPUTE_WORKERS:-2}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRE_MINUTES=${JWT_EXPIRE_MINUTES:-720}
      - FRONTEND_ORIGIN=https://dash-tg.arvsystems.cloud
      - SECURE_COOKIES=true
    networks:
      - network_public
    volumes:
      - /opt/api-esoccer/data:/app/data:ro
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      labels:
        - traefik.enable=true
        - traefik.docker.network=network_public
        - traefik.http.routers.api_esoccer.rule=Host(`api-tg-esoccer.arvsystems.cloud`)
        - traefik.http.routers.api_esoccer.entrypoints=websecure
        - traefik.http.routers.api_esoccer.tls.certresolver=letsencryptresolver
        - traefik.http.services.api_esoccer.loadbalancer.server.port=8000

  frontend:
    image: ${IMAGE_FRONTEND}:latest
    networks:
      - network_public
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      labels:
        - traefik.enable=true
        - traefik.docker.network=network_public
        - traefik.http.routers.esoccer_dash.rule=Host(`dash-tg.arvsystems.cloud`)
        - traefik.http.routers.esoccer_dash.entrypoints=websecure
        - traefik.http.routers.esoccer_dash.tls.certresolver=letsencryptresolver
        - traefik.http.services.esoccer_dash.loadbalancer.server.port=80

  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - network_public
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      placement:
        constraints:
          - node.role == manager

networks:
  network_public:
    external: true

volumes:
  redis_data:
